[build]
  base = "apps/landing"
  command = "cd ../.. && turbo run build --filter=@threadmark/landing"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
